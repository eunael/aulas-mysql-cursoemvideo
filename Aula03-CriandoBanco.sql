CREATE DATABASE cadastro; #depois que criar o BD não precisa criar dnv

create table pessoas( #depois que criar uma tabela não precisa criar dnv
nome varchar(30),
idade tinyint(3),
sexo char(1),
peso float,
altura float,
nacionalidade varchar(20)
);
/*describe pessoas; descrever a tabela pessoa */
DROP DATABASE cadastros; #exclui um Banco de Dados
